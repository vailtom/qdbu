{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissoes da janela principal. Enxuta de proposito: so o que o app usa de fato -- ver docs/09-modelo-de-confianca.md.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
